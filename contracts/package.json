{
  "name": "trutharena-contracts",
  "version": "1.0.0",
  "description": "TruthArena Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:local": "hardhat run deploy.js --network localhost",
    "deploy:sepolia": "hardhat run deploy.js --network mantleSepolia",
    "deploy:mainnet": "hardhat run deploy.js --network mantleMainnet",
    "test": "hardhat test",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  }
}
